# MySQL connection
spring.datasource.url=jdbc:mysql://victorgranados.com:3306/victorgr_app_mkm?useSSL=false&serverTimezone=UTC
spring.datasource.username=victorgr_ji
spring.datasource.password=56sOu9pW4c
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080
spring.thymeleaf.cache=false
spring.thymeleaf.check-template-location=false




# Tiempo máximo de vida de una conexión en el pool (30 minutos por defecto)
spring.datasource.hikari.max-lifetime=1800000

# Tiempo de inactividad antes de que una conexión sea elegible para ser removida del pool (10 minutos)
spring.datasource.hikari.idle-timeout=600000

# Tiempo máximo para validar una conexión (en milisegundos)
spring.datasource.hikari.validation-timeout=5000

# Número mínimo de conexiones inactivas en el pool
spring.datasource.hikari.minimum-idle=2

# Número máximo de conexiones en el pool
spring.datasource.hikari.maximum-pool-size=10

# Tiempo de mantenimiento de la conexión viva en el pool (5 minutos)
spring.datasource.hikari.keepalive-time=300000



